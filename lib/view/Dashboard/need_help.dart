import 'package:flutter/material.dart';

class NeedHelp extends StatelessWidget {
  static const String id = 'need_help';
  const NeedHelp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
